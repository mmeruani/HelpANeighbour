enum RequestUrgency {
  urgent,
  flexible,
}

enum RewardType {
  fixed,
  negotiable,
  none,
}

enum RequestStatus {
  active,
  inProgress,
  awaitingCustomerConfirmation,
  completed,
  cancelled,
}
